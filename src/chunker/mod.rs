//use crate::core::{Chunk, Result};
// use async_trait::async_trait;
// use tokio::sync::mpsc;
// use uuid::Uuid;

// #[async_trait]
// pub trait Chunker: Send + Sync {
//     async fn chunk(&self, content: &str, source_id: Uuid, tx: mpsc::Sender<Chunk>) -> Result<()>;
// }
